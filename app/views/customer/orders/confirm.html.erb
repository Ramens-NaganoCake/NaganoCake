<h2>注文情報確認</h2>
<div calss="row">
<div class="col-xs-9">
 <% if current_customer.cart_items.present? %>
 <table class="table table-bordered table-condensed">
  <thead>
    <tr>
        <th>商品名</th>
        <th>単価（税込）</th>
        <th>数量</th>
        <th>小計</th>
    </tr>
    </thead>
    <tbody>
     <% @cart_items.each do |cart| %>
      <tr>
        <td>
          <%= attachment_image_tag(cart.item, :image, size: "60x50", fallback: "no_image.jpg") %>  
          <%= cart.item.name %>
        </td>
        <td>
          ¥<%= tax_price(cart.item.price).to_s(:delimited) %>
        </td>
        <td><%= cart.quantity %></td>
        <td>¥<%= sub_price(cart).to_s(:delimited) %></td>
      </tr>
     <% end %>
    </tbody>
 </table>
 </div>
 <div class="col-xs-3">
 <table>
    <tr>
        <th>送料</th>
        <td><%= @order.postage %></td>
    </tr>
    <tr>
        <td>商品合計</td>
        <td><%= total_price(@cart_items).to_s(:delimited) %></td>
    </tr>
    <tr>
        <td>請求金額</td>
        <td><%= billing(@order).to_s(:delimited) %></td>
    </tr>
 </table>
  
 </div>
 <div class="col-xs-9">
  <table>
     <tr>
         <th>支払方法</th>
         <td><%= @order.payment %></td>
     </tr>
     <tr>
         <th>お届け先</th>
         <td><%= "#{@order.postcode} #{@order.address} #{@order.destination}" %></td>
     </tr>
  </table>
  
  <div class ="col-xs-11">
      <%= form_with model:@order, url:customer_orders_path, method: :post, local: true do |f| %>
        <%= f.hidden_field :postcode %>
        <%= f.hidden_field :address %>
        <%= f.hidden_field :destination %>
        <%= f.hidden_field :payment %>
        <%= f.hidden_field :tax_price %>
        <%= f.hidden_field :delivery, value:@delivery %>
        <%= f.submit "購入を確定する", class: "btn btn-info" %>
      <% end %>
  </div>
    <% else %>
        <!-- if カートアイテムがない時 -->
        <p>お客様のショッピングカートに商品はありませんので注文確定ができません。</p>
        <p><%= link_to "商品一覧", customer_items_path %>から商品をお探し下さい。</p>
    <% end %>
 </div>
 </div>
