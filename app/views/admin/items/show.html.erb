<div class="container">
  <div class="row">
    <div class="mt-5 col-md-7">
      <h1 class="mb-4 col-md-5 offset-md-5">商品詳細</h1>
        <span class="col-md-5 offset-md-5"><%= attachment_image_tag @item, :image, format: 'jpeg', fallback: 'no_image.jpg', size:'200x150'%></sapn>
    </div>
    <div class="mt-5 col-md-5">
        <p>商品名 <%= @item.name %></p>
        <p>商品説明 <%= @item.introduction %></p>
        <p>ジャンル<%= @item.genre.name %></p>
        <p>税込価格（税抜価格）<%= (@item.price * 1.1).floor.to_s(:delimited) %>円(<%= @item.price.to_s(:delimited) %>円)</p>
        <p>販売ステータス
          <% if @item.is_active == true %>
            <p class="text-success">販売中</p>
          <% elsif @item.is_active == false %>
            <p class="text-muted">販売停止中</p>
          <% end %>
        </p>
          <%= link_to "編集する", edit_admin_item_path(@item.id), class: "btn btn-success btn-lg pull-right" %>
    </div>
  </div>
</div>