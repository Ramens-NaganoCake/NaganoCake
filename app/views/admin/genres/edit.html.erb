<h3 class="head-line">ジャンル編集</h3>
<%= form_with model: @genre, url: admin_genre_path, local: true do |f| %>
<p>ジャンル名 <%= f.text_field :name %> <%= f.submit '変更を保存', class: "glyphicon glyphicon-pencil btn btn-success" %></p>
<% end %>